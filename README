说明：
人机对弈或不同方法之间对弈：python main.py
从头开始训练：python train_nn.py
models 文件夹：训练好的模型

需要的运行环境：
操作系统：Windows或Linux（人机对弈时需要图形化用户界面）
Python版本：Python 3.5

需要的第三方库：
tensorflow 1.1.0
    可通过 pip install tensorflow==1.1.0 命令（不带gpu支持）
    或 pip install tensorflow-gpu==1.1.0 命令安装（带有gpu支持，若安装此版本还需要有支持CUDA的gpu，以及该gpu的驱动程序和CUDA 8）
numpy 1.14.0
    通过pip安装tensorflow时会自动安装此依赖，也可通过 pip install numpy==1.14.0安装
tkinter
    Windows版本的Python 3.5一般自带tkinter库并能正常使用
    Ubuntu操作系统可通过 sudo apt-get install python-tk 命令安装